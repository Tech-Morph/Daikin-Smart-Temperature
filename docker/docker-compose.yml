version: '3.9'

services:
  smart-brain:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: smart-learning-house
    restart: unless-stopped
    volumes:
      - ../config/config.yaml:/app/config/config.yaml:ro
      - ../data:/app/data
    environment:
      - TZ=America/Los_Angeles
    network_mode: host   # Needed for Daikin LAN API access
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
