{
  "config": {
    "step": {
      "user": {
        "title": "Daikin Comfort Control Smart Temperature",
        "description": "Select the Daikin device to control automatically.",
        "data": {
          "device_id": "Daikin Device"
        }
      }
    },
    "abort": {
      "no_daikin_devices": "No Daikin devices found. Install daikin_comfort_control first.",
      "already_configured": "Smart temperature is already configured for this device."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Smart Temperature Settings",
        "data": {
          "target_temp": "Target temperature (°F)",
          "tolerance": "Tolerance band (°F)",
          "min_temp": "Minimum temperature (°F)",
          "max_temp": "Maximum temperature (°F)",
          "allow_cool": "Allow cooling",
          "allow_heat": "Allow heating",
          "allow_fan_only": "Allow fan-only mode",
          "max_fan_mode": "Maximum fan speed",
          "season_mode": "Season mode",
          "summer_heat_min_temp": "Summer: minimum indoor temp before heating (°F)",
          "summer_heat_night_only": "Summer: allow heating only at night",
          "outdoor_heat_max": "Summer: max outdoor temp to allow heating (°F)",
          "precool_enabled": "Enable outdoor-trend pre-cooling",
          "precool_rise_threshold": "Pre-cool trigger: outdoor rise over 30 min (°F)",
          "precool_tolerance_cut": "Pre-cool tolerance reduction (°F)",
          "safety_override_delta": "Safety bypass: force correction if delta exceeds (°F)",
          "learning_log_enabled": "Enable rolling learning log",
          "learning_log_size": "Learning log max entries",
          "learning_enabled": "Enable time-of-day offsets",
          "morning_offset": "Morning offset 6am–9am (°F)",
          "day_offset": "Day offset 9am–5pm (°F)",
          "evening_offset": "Evening offset 5pm–10pm (°F)",
          "night_offset": "Night offset 10pm–6am (°F)",
          "fan_close_delta": "Low fan threshold (°F from target)",
          "fan_mid_delta": "Medium fan threshold (°F from target)",
          "poll_interval": "Poll interval (seconds)",
          "mode_switch_min": "Min seconds between mode switches",
          "override_timeout": "Manual override pause (seconds, 0=disabled)"
        }
      }
    },
    "error": {
      "invalid_temp_range": "Minimum temperature cannot be greater than maximum temperature.",
      "at_least_one_hvac_mode": "At least one HVAC mode must be enabled."
    }
  }
}
